{
  "permissions": {
    "allow": [
      "Bash(bash .claude/agents/lib/cheap-coder-run.sh:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -qF '.claude/agents/lib/cheap-coder-run.sh' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"allow\",\"permissionDecisionReason\":\"cheap-coder engine script (auto-approved by project hook)\"}}' || true"
          }
        ]
      }
    ]
  }
}
